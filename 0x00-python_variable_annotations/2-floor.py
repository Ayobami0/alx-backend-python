#!/usr/bin/python3
"""Contains functions that returns the floor of a float"""


def floor(n: float) -> int:
    """Converts a floating point number to its floor version.

    Param:
        n (float): the number to convert
    Return:
        int: the floor of the float
    """
    import math

    return math.floor(n)
